import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    alignItems: 'center', backgroundColor: '#dff4ff', borderColor: '#77c4df', borderRadius: 999,
    borderWidth: 2, justifyContent: 'center',
  },
});
